// Change this path to the directory and HTML file you want to redirect to
const redirectPath = "./per/index.html";

// Perform the redirect
setTimeout(() => {window.location.href = redirectPath},3000);
